<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>HireIQ - Enhance Your Hiring Experience</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'dark-blue': '#050A19',
                        'purple-glow': '#6B46C1',
                        'blue-button': '#4F46E5',
                    },
                    animation: {
                        'pulse-slow': 'pulse 4s ease-in-out infinite',
                        'glow': 'glow 2s ease-in-out infinite alternate',
                        'float': 'float 6s ease-in-out infinite',
                    },
                    keyframes: {
                        glow: {
                            '0%': { boxShadow: '0 0 10px rgba(139, 92, 246, 0.2)' },
                            '100%': { boxShadow: '0 0 25px rgba(139, 92, 246, 0.5)' }
                        },
                        float: {
                            '0%, 100%': { transform: 'translateY(0)' },
                            '50%': { transform: 'translateY(-20px)' }
                        }
                    }
                }
            }
        }
    </script>
    <style>
        body {
            background-color: #050A19;
        }

        .grid-background {
            background-image: linear-gradient(to right, rgba(91, 33, 182, 0.1) 1px, transparent 1px),
            linear-gradient(to bottom, rgba(91, 33, 182, 0.1) 1px, transparent 1px);
            background-size: 40px 40px;
        }

        .gradient-bottom {
            background: linear-gradient(to top, rgba(91, 33, 182, 0.2), transparent);
        }

        .glow-circle {
            position: absolute;
            border-radius: 50%;
            filter: blur(80px);
            z-index: -1;
        }

        @media (max-width: 640px) {
            .glow-circle {
                filter: blur(60px);
            }
        }
    </style>
</head>
<body class="text-white grid-background min-h-screen relative overflow-x-hidden">

<div class="glow-circle w-96 h-96 bg-purple-700/30 -top-20 -left-20 animate-pulse-slow"></div>
<div class="glow-circle w-96 h-96 bg-indigo-600/20 bottom-0 right-0 animate-pulse-slow"></div>
<div class="glow-circle w-64 h-64 bg-violet-800/25 top-1/2 left-1/3 animate-float"></div>

<div class="absolute bottom-0 left-0 right-0 h-64 gradient-bottom w-full z-0 animate-pulse-slow"></div>

<div class="container mx-auto px-4 sm:px-6 lg:px-8 mt-6 sm:mt-10 relative z-10">
    <header class="flex items-center justify-between">
        <div class="flex items-center space-x-2">
            <span class="font-bold text-2xl sm:text-3xl animate-glow text-white">HireIQ</span>
        </div>

        <div class="flex items-center space-x-4 sm:space-x-6">
            <a href="/login" class="text-gray-300 hover:text-white transition duration-200 hover:scale-105">Login</a>
            <a href="/signup"
               class="bg-blue-button text-white px-4 sm:px-6 py-2 rounded-full font-medium transition duration-300 ease-in-out hover:scale-105 shadow-lg hover:shadow-blue-500/30 animate-glow">
                Sign up
            </a>
        </div>
    </header>

    <section class="py-12 sm:py-16 md:py-24 max-w-5xl mx-auto text-center">
        <div class="inline-flex items-center space-x-2 bg-dark-blue border border-gray-800 px-4 sm:px-5 py-2 rounded-full mb-8 sm:mb-12 shadow-md animate-glow">
            <div class="w-3 h-3 sm:w-4 sm:h-4 bg-purple-600 rounded-sm shadow-md"></div>
            <span class="text-sm sm:text-base text-white">AI Powered Recruitment Technology</span>
        </div>

        <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold mb-6 leading-tight">
            Enhance Your Hiring<br/>
            Experience with <span class="bg-clip-text text-transparent bg-gradient-to-r from-purple-glow to-blue-button">ATS</span>
        </h1>

        <p class="text-base sm:text-lg text-gray-300 max-w-3xl mx-auto mb-10 sm:mb-12 px-4">
            Revolutionize your hiring process with our transformative Applicant Tracking System and unlock the power of streamlined recruitment.
        </p>

        <div class="mb-12 sm:mb-16">
            <a href="/get-started"
               class="inline-block bg-blue-button text-white px-8 sm:px-10 py-3 rounded-full text-base sm:text-lg font-medium transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-blue-400/40 animate-glow">
                Get Started
            </a>
        </div>

        </section>
</div>
<% if (typeof toastMessage !== 'undefined' && toastMessage) { %>
    <script>
        Toastify({
            text: "<%= toastMessage %>",
            duration: 4000,
            gravity: "top",
            position: "right",
            close: true,
            style: {
                background: "<%= toastType === 'error' ? 'linear-gradient(to right, #ef4444, #b91c1c)' : 'linear-gradient(to right, #22c55e, #15803d)' %>",
                color: "#fff",
                borderRadius: "8px",
                boxShadow: "0 0 20px rgba(0,0,0,0.3)"
            }
        }).showToast();
    </script>
<% } %>

</body>
</html>